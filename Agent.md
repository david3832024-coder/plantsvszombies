# Agent Instructions

- When working on PPT, PowerPoint, `.pptx`, slides, deck, or presentation tasks, use the locally installed Anthropic `pptx` skill by default unless the user explicitly requests a different skill or workflow.
- The Anthropic `pptx` skill is installed at `/Users/macforai/.agents/skills/pptx`.
